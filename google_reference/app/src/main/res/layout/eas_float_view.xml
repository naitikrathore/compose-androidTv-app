<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginTop="30dp"
    android:layout_marginBottom="30dp"
    android:orientation="vertical"
    android:padding="10dp">

    <TextView
        android:id="@+id/nav_eas_alert"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/eas_bg_selected"
        android:ellipsize="marquee"
        android:marqueeRepeatLimit="marquee_forever"
        android:maxLines="1"
        android:gravity="center"
        android:padding="@dimen/custom_dim_5"
        android:scrollHorizontally="true"
        android:singleLine="true"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:textStyle="bold"
        tools:text="@string/channels_scan_select_description"
        android:visibility="invisible" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">

        <Button
            android:id="@+id/btn_eas_control"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_gravity="end"
            android:layout_marginBottom="10dp"
            android:background="@drawable/eas_control_button_icon"
            android:clickable="true"
            android:focusable="true"
            android:text="@string/atsc30_hide_eas"
            android:textColor="#fff"
            android:textSize="20sp"
            android:visibility="invisible" />

        <TextView
            android:id="@+id/nav_eas_activation"
            android:layout_width="fill_parent"
            android:layout_height="30dp"
            android:background="@drawable/eas_control_button_icon"
            android:ellipsize="marquee"
            android:fadingEdge="horizontal"
            android:focusable="true"
            android:focusableInTouchMode="true"
            android:gravity="center"
            android:marqueeRepeatLimit="marquee_forever"
            android:scrollHorizontally="true"
            android:singleLine="true"
            android:textColor="#fff"
            android:textSize="20sp"
            android:visibility="invisible" />

    </LinearLayout>

</RelativeLayout>